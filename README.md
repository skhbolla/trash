# poshell
POSIX-compliant shell built to explore the Linux Process API. This project implements the core mechanics of a command-line interface, including process creation via fork/exec, pipeline management with pipe, and asynchronous event handling. Designed as a deep-dive into how the kernel manages user-space execution environments.
